# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/

# Mason — hook package cache + compiled hook output (local only)
bricks/**/hooks/.dart_tool/
bricks/**/hooks/build/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Environment configuration files
# .env.example should be committed as a template
.env
.env.local
.env.*.local

# fastlane run artifacts (both fastlane/ and ios/fastlane/). fastlane also
# regenerates its own README.md on every run; that one is not tracked either.
**/fastlane/report.xml
**/fastlane/Preview.html
**/fastlane/screenshots
**/fastlane/test_output
ios/fastlane/README.md

# Code signing material - never commit any of it.
# android/.gitignore already covers key.properties and Android keystores; these
# patterns cover the Apple side and re-cover keystores from the repo root,
# because .github/workflows/deploy-ios.yml's (commented) signing step writes
# certificate.p12 into the working directory, which is the repo root.
*.p12
*.p8
*.mobileprovision
*.provisionprofile
*.cer
*.certSigningRequest
*.keystore
*.jks

# ios/Podfile and macos/Podfile are deliberately COMMITTED, not ignored:
# `flutter pub get` regenerates them when absent, which made them permanent
# untracked churn every session had to hand-revert, and left the CocoaPods
# platform floating. `Pods/` and the ephemeral Flutter configs stay ignored via
# ios/.gitignore and macos/.gitignore.

# Antigravity AI
.agent/

# Acceptance evidence is deliberately committed: the issue-driven workflow closes
# an issue only against retrievable artifacts (docs/issue-workflow.md,
# invariant 4). Without this negation the `*.log` rule above silently swallows
# every log written by scripts/test/run_acceptance.sh, and `git add <dir>` skips
# ignored files without erroring - so the evidence links would 404.
!docs/verification/**/*.log
