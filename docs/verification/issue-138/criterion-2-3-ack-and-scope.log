Criterion 2: the inline env-asset-ack: exception still applies.
Criterion 3: the widened match set, and its stated boundary.
Repository HEAD: ef94605

=== A. file entry acknowledgement (unchanged from #79) ===

$ cat /tmp/pubspec-file-ack.yaml
name: demo
flutter:
  assets:
    - .env.example
    - .env # env-asset-ack: web build, contains no secrets
exit=0

$ dart run tool/check_env_assets.dart /tmp/pubspec-file-ack.yaml
Running build hooks...Running build hooks...check_env_assets: /tmp/pubspec-file-ack.yaml:5 bundles ".env" with an acknowledgement. It ships in every build mode and is public - keep secrets out of it.
OK: no unacknowledged secrets file in the Flutter asset list, and none inside a declared asset directory.
exit=0

=== B. directory acknowledgement must NAME the file ===

$ grep -n -A4 ^  assets: pubspec-ack-demo.yaml
172:  assets:
173-    - .env.example
174-    - assets/images/
175-    - assets/config/ # env-asset-ack: .env.web, publishable values only
176-
exit=0

$ ls -a assets/config
.
..
.env.production
.env.web
.gitkeep
exit=0

B1. .env.web is acknowledged by name, .env.production is not: still fails

$ dart run tool/check_env_assets.dart pubspec-ack-demo.yaml
Running build hooks...Running build hooks...check_env_assets: pubspec-ack-demo.yaml:175 bundles "assets/config/.env.web" with an acknowledgement. It ships in every build mode and is public - keep secrets out of it.
SECURITY: a secrets file is bundled as a Flutter asset.

  pubspec-ack-demo.yaml:175  - assets/config/.env.production (via "assets/config/")  [environment file]

Flutter asset lists are not build-mode scoped, so this file ships in the
release APK, the release IPA and the web build, not just in debug. Anyone can
unzip the app bundle to read it, and on web it is served over HTTP at
/assets/<path> and listed in AssetManifest.json. A declared *directory* bundles
everything directly inside it, with no pubspec entry per file.

Use instead:
  * native (all build modes): --dart-define-from-file=.env, or individual
    --dart-define flags. Nothing is bundled as a readable file.
  * web: there is no dart-define path, and a web client cannot hold a secret.
    Serve secrets from your backend and keep only publishable values in the
    bundle.
  * key and signing material: keep it out of the tree the app is built from.
    Nothing an app bundle contains is private.

If the file genuinely holds no secrets and must ship (a web build needing
runtime config), say so inline and this check will pass:

    - .env # env-asset-ack: web build, contains no secrets

For a file inside a declared directory, put the acknowledgement on the
directory entry and name the file, so it covers that file and not the whole
directory:

    - assets/config/ # env-asset-ack: app_config.env, publishable values only

See docs/guides/configuration.md ("Never ship a secret in the bundle").

exit=1

B2. remove the unacknowledged file: the acknowledged one passes with a
    reminder printed on stdout, exactly as a file entry does

$ dart run tool/check_env_assets.dart pubspec-ack-demo.yaml
Running build hooks...Running build hooks...check_env_assets: pubspec-ack-demo.yaml:175 bundles "assets/config/.env.web" with an acknowledgement. It ships in every build mode and is public - keep secrets out of it.
OK: no unacknowledged secrets file in the Flutter asset list, and none inside a declared asset directory.
exit=0

B3. the same tree WITHOUT the acknowledgement (the repository's own
    pubspec) still fails - the marker is doing the work, not the file name

$ dart run tool/check_env_assets.dart pubspec.yaml
Running build hooks...Running build hooks...SECURITY: a secrets file is bundled as a Flutter asset.

  pubspec.yaml:175  - assets/config/.env.web (via "assets/config/")  [environment file]

Flutter asset lists are not build-mode scoped, so this file ships in the
release APK, the release IPA and the web build, not just in debug. Anyone can
unzip the app bundle to read it, and on web it is served over HTTP at
/assets/<path> and listed in AssetManifest.json. A declared *directory* bundles
everything directly inside it, with no pubspec entry per file.

Use instead:
  * native (all build modes): --dart-define-from-file=.env, or individual
    --dart-define flags. Nothing is bundled as a readable file.
  * web: there is no dart-define path, and a web client cannot hold a secret.
    Serve secrets from your backend and keep only publishable values in the
    bundle.
  * key and signing material: keep it out of the tree the app is built from.
    Nothing an app bundle contains is private.

If the file genuinely holds no secrets and must ship (a web build needing
runtime config), say so inline and this check will pass:

    - .env # env-asset-ack: web build, contains no secrets

For a file inside a declared directory, put the acknowledgement on the
directory entry and name the file, so it covers that file and not the whole
directory:

    - assets/config/ # env-asset-ack: app_config.env, publishable values only

See docs/guides/configuration.md ("Never ship a secret in the bundle").

exit=1

=== C. what the widened rule fires on inside a declared directory ===

$ ls -a assets/config
.
..
.gitkeep
app_config.json
apple_dist.cer
google-services.json
logo.png
pinned_root.crt
secrets.json
server.pem
upload.p12
exit=0

Expect: p12, pem, secrets.json, google-services.json flagged.
Expect: app_config.json, logo.png, .crt, .cer NOT flagged - a certificate
        is public by design, and firing on config.json inside a directory
        named assets/config/ is how a guard gets switched off.

$ dart run tool/check_env_assets.dart
Running build hooks...Running build hooks...SECURITY: a secrets file is bundled as a Flutter asset.

  pubspec.yaml:175  - assets/config/google-services.json (via "assets/config/")  [credential file]
  pubspec.yaml:175  - assets/config/secrets.json (via "assets/config/")  [credential file]
  pubspec.yaml:175  - assets/config/server.pem (via "assets/config/")  [private key or code-signing material]
  pubspec.yaml:175  - assets/config/upload.p12 (via "assets/config/")  [private key or code-signing material]

Flutter asset lists are not build-mode scoped, so this file ships in the
release APK, the release IPA and the web build, not just in debug. Anyone can
unzip the app bundle to read it, and on web it is served over HTTP at
/assets/<path> and listed in AssetManifest.json. A declared *directory* bundles
everything directly inside it, with no pubspec entry per file.

Use instead:
  * native (all build modes): --dart-define-from-file=.env, or individual
    --dart-define flags. Nothing is bundled as a readable file.
  * web: there is no dart-define path, and a web client cannot hold a secret.
    Serve secrets from your backend and keep only publishable values in the
    bundle.
  * key and signing material: keep it out of the tree the app is built from.
    Nothing an app bundle contains is private.

If the file genuinely holds no secrets and must ship (a web build needing
runtime config), say so inline and this check will pass:

    - .env # env-asset-ack: web build, contains no secrets

For a file inside a declared directory, put the acknowledgement on the
directory entry and name the file, so it covers that file and not the whole
directory:

    - assets/config/ # env-asset-ack: app_config.env, publishable values only

See docs/guides/configuration.md ("Never ship a secret in the bundle").

exit=1

=== D. the scope is stated in the tool header (criterion 3) ===

$ sed -n 25,33p;54,66p tool/check_env_assets.dart
// What counts as a secrets file (see `secretAssetReason`): env files, private
// key and code-signing material by extension, and a short list of exact
// credential file names. It deliberately does **not** try to judge whether an
// arbitrary config file holds a secret - a rule that fired on `config.json`
// inside a directory literally named `assets/config/` would be wrong on its
// first run and would be deleted rather than fixed. Public certificates
// (`.cer`, `.crt`, `.der`) are likewise not flagged: a certificate is public by
// design and a pinning setup may legitimately ship one.
//
//
// What this cannot catch:
//
//   * a file created after the check runs. The directory walk sees the tree as
//     it is at check time, and nothing re-runs it at build time - see
//     koniz-dev/flutter-starter#135 for the release-build path;
//   * a secret inside a file whose name looks innocuous. Matching is by name
//     only; nothing reads file contents;
//   * a secret in a *sub*-directory of a declared directory, which Flutter does
//     not bundle either (each sub-directory needs its own asset entry, and the
//     walk then covers it).
//
// Usage:
exit=0

=== E. tree restored ===

$ ls -a assets/config
.
..
.gitkeep
exit=0

$ dart run tool/check_env_assets.dart
Running build hooks...Running build hooks...OK: no unacknowledged secrets file in the Flutter asset list, and none inside a declared asset directory.
exit=0

$ git status --porcelain   # only flutter pub get churn and the evidence dir
 M analysis_options.yaml
 M ios/Flutter/Debug.xcconfig
 M ios/Flutter/Release.xcconfig
 M macos/Flutter/Flutter-Debug.xcconfig
 M macos/Flutter/Flutter-Release.xcconfig
 M pubspec.lock
?? docs/verification/issue-138/
