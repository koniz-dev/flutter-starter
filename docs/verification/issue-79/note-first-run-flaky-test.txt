Note: the FIRST acceptance run on main at dd0e6af failed one test. It is
unrelated to issue #79 and is intermittent. Recorded here rather than quietly
re-running, because a green second run on its own would hide it.

Failing test:
  test/core/network/interceptors/auth_interceptor_refresh_queue_test.dart
  "AuthInterceptor 401 refresh queue three concurrent 401s with a failing
   refresh: all three complete"

Excerpt from that run:

00:34 +404 ~1 -1: .../auth_interceptor_refresh_queue_test.dart: AuthInterceptor
  401 refresh queue three concurrent 401s with a failing refresh: all three
  complete [E]
  Expected: <1>
    Actual: <3>

  package:matcher                                                expect
  package:flutter_test/src/widget_tester.dart 473:18             expect
  test/core/network/interceptors/auth_interceptor_refresh_queue_test.dart 172:9

Why it is not this issue's:

1. Commit cd56d01 changes no file under lib/. It touches documentation,
   the pubspec comment, scripts/dev/audit_template.sh, tool/check_env_assets.dart
   and test/tooling/check_env_assets_test.dart only - see
   criterion-5-no-secrets.txt for the full stat. It cannot affect the behaviour
   of an HTTP interceptor.
2. The CI Quality gate on PR #126 ran the whole suite on the rebased head and
   passed (run 35445713908).
3. The test file passes on its own, run twice back to back on the same
   worktree and the same commit:
     $ flutter test test/core/network/interceptors/auth_interceptor_refresh_queue_test.dart
     00:14 +6: All tests passed!
     00:10 +6: All tests passed!
4. The second full acceptance run passed: 2486 passed, 6 skipped, exit 0.
   See tests.log and criterion-6-audit_template.log.

Filed separately as koniz-dev/flutter-starter#134 (epic:core-network) rather
than fixed here: this issue is epic:core-security, and the loop forbids a
drive-by fix in another epic's slice.
