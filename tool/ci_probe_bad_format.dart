/// Throwaway probe for koniz-dev/flutter-starter#117.
///
/// Deliberately misformatted so `dart format --set-exit-if-changed` fails and
/// the Quality gate turns red on a code change. This file is never merged.
int probeAdd(   int a,int b ) {
      return a+b;
}
